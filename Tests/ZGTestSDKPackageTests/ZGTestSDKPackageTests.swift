import XCTest
@testable import ZGTestSDKPackage

final class ZGTestSDKPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ZGTestSDKPackage().text, "Hello, World!")
    }
}
